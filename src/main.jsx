import React from 'react'
import ReactDOM from 'react-dom/client'
import BarakahPOS from '../barakah-pos.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BarakahPOS />
  </React.StrictMode>,
)
